QREDOCHAIN_DOCKER_VERSION="<qredochain_docker_image>"            ## Qredochain Docker image version
NETWORK="<testnet or mainnet>"                                   ## Network to use for qredochain (testnet, mainnet)
SNAPSHOTS_PASSWORD="base64(username:password)"                   ## Base64 encoded username and password, with the format username:password, to get the snapshots
LOCAL_DATA_DIR="/Path/to/local/directory"                        ## Local directory to save the data generated by the qredochain docker container.